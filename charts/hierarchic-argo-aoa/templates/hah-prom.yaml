apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  {{- if and (.Values.global.dest.name) (not .Values.namePrefix ) }}
  name: {{ .Values.global.dest.name }}-{{ .Values.prom.name }}
  {{- else if (.Values.namePrefix) }}
  name: {{ .Values.namePrefix }}-{{ .Values.prom.name }}
  {{- end }}
  namespace: {{ .Values.global.argoNamespace }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: {{ .Values.prom.namespace | default .Values.global.dest.namespace | quote }}
    {{- if .Values.global.dest.server }}
    server: {{ .Values.global.dest.server }}
    {{- else if and (.Values.global.dest.name) (not .Values.global.dest.server) }}
    name: {{ .Values.global.dest.name }}
    {{- end }}
  project: {{ .Values.prom.project | default .Values.global.project }}
  source:
    repoURL: {{ .Values.prom.repoURL | default .Values.global.chartDefaults.repoURL | quote }}
    targetRevision: {{ .Values.prom.revision | default .Values.global.chartDefaults.revision | quote }}
    chart: {{ .Values.prom.chart_name }}
    {{- if .Values.prom.valueOverrides }}
    helm:
      values:
        {{ .Values.prom.valueOverrides | toYaml | trim | indent 10 }}
    {{ end }}
